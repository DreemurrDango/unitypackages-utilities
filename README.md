# 集合功能扩展类

## 概述

此插件提供了一些基于C#集合的集合类结构的扩展方法，以及扩展集合类，当前包括以下部分：

*   用于优先级排序的二叉堆(Heap)，包括最小堆与最大堆
    
*   双向字典(BiDictionary)，用于判定等效对应数据
    
*   对数组和列表方法的洗牌随机算法扩展
    

## 使用方法

引入DreemurrStudio.CollectionExtension命名空间，实例化所需的集合实例即可（洗牌随机算法无须手动导入，只需调用数据实例的.Shuffle方法即可）